import type { Metadata } from "next";
import { Athiti, Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const athiti = Athiti({
  weight: ["300", "400", "500", "600", "700"],
  variable: "--font-athiti",
  subsets: ["thai"],
});

export const metadata: Metadata = {
  title: "Event Around",
  description: "Generated by Suphanat Chanlek",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${athiti.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
